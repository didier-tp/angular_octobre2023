import { VeryBasicFilterPipe } from './very-basic-filter.pipe';

describe('VeryBasicFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VeryBasicFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
